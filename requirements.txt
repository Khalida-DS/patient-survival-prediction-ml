# Runtime dependencies for the Streamlit demo ONLY (app/app.py).
# Pinned to exact versions verified to load models/calibrated_gb_model.pkl
# (saved with scikit-learn 1.8.0) without errors. Do not loosen these
# pins without re-testing against the committed model file — an
# unpinned scikit-learn was the root cause of the original deploy
# failure (see docs/GUIDE.md, "What was broken and why").
#
# Training, experiment tracking, drift monitoring, and testing tools
# live in requirements-dev.txt, not here, to keep the Streamlit Cloud
# install small and fast.
streamlit==1.64.0
pandas==3.0.6
numpy==2.3.5
scikit-learn==1.8.0
shap==0.52.0
plotly==7.1.0
PyYAML==6.0.3
